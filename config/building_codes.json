{
  "stud_spacing": {
    "standard_spacing_mm": 406,
    "tolerance_mm": 13,
    "rules": [
      {
        "id": "STUD_SPACING_001",
        "description": "Interior studs must be 16\" on center ±0.5\"",
        "deterministic": true,
        "severity": "high"
      }
    ]
  },
  "openings": {
    "rules": [
      {
        "id": "WINDOW_SUPPORT_001",
        "description": "Windows < 36\" wide require jack studs and header",
        "width_threshold_mm": 914,
        "requires_jack_studs": true,
        "requires_header": true,
        "deterministic": true,
        "severity": "critical"
      }
    ]
  },
  "mep_clearance": {
    "duct_to_stud_mm": 38,
    "tolerance_mm": 2,
    "rules": [
      {
        "id": "DUCT_CLEARANCE_001",
        "description": "Ducts must be 1.5\" minimum from studs",
        "deterministic": true,
        "severity": "high"
      }
    ]
  },
  "seismic_requirements": {
    "rules": [
      {
        "id": "SEISMIC_BRACING_001",
        "description": "Corner openings in seismic zones require enhanced bracing",
        "deterministic": false,
        "llm_review_required": true,
        "severity": "high"
      }
    ]
  }
}
